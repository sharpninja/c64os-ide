# OBSOLETE PATCH - Superseded by upstream solution in commit 5ca7ce898bca1a3696dbc9e444207026eabd58d5
#
# This patch modified src/Makefile to handle encoding validation more gracefully.
#
# The VICE Team has replaced this entire section with a better solution in src/Makefile.am:
# - Now uses error handling with `|| { fallback }` to provide a minimal infocontrib.h on failure
# - Eliminates the need for complex encoding validation
# - More robust and maintainable approach
#
# Current upstream code (as of commit 5ca7ce898b):
# @$(SHELL) $(srcdir)/buildtools/geninfocontrib_h.sh infocontrib.h <$(top_srcdir)/doc/vice.texi [...] 2>/dev/null || { \
#     echo "WARNING: Could not generate infocontrib.h, using minimal version"; \
#     rm -f infocontrib.h; \
#     printf '#ifndef VICE_INFOCONTRIB_H\n[...]\n' > infocontrib.h; \
# }
#
# Historical patch content (no longer applicable):
# --- a/src/Makefile
# +++ b/src/Makefile
# ... (patch omitted, no longer needed)
