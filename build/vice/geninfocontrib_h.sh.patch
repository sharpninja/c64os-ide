# OBSOLETE PATCH - Applied upstream as of commit 5ca7ce898bca1a3696dbc9e444207026eabd58d5
#
# This patch added file existence checks before cat operations in geninfocontrib_h.sh
# to prevent errors when temp files don't exist.
#
# The VICE Team has applied these fixes upstream using a different but equivalent approach:
# - Uses `if [ -f file ]; then cat file; fi` instead of `test -f file && cat file`
# - Applied in multiple commits during 2025-2026 refactoring
#
# Historical patch content:
# --- a/src/buildtools/geninfocontrib_h.sh
# +++ b/src/buildtools/geninfocontrib_h.sh
# @@ -299,19 +299,19 @@ if test x"$1" = "xinfocontrib.h"; then
#
#    $ECHO ""
#    $ECHO "vice_team_t core_team[] = {"
# -  cat coreteam.tmp | sed "s/__VICE_CURRENT_YEAR__/$year/g"
# +  test -f coreteam.tmp && cat coreteam.tmp | sed "s/__VICE_CURRENT_YEAR__/$year/g"
#    rm -f coreteam.tmp
# ... (rest of patch omitted, no longer needed)
